module SpaceCombat.Util {
    /**
     * Contains info on the mouse state.
     */
    export class MouseInput {
        x: number;
        y: number;
        /**
         * Whether the left button is pressed.
         */
        lbPressed: boolean;
        /**
         * Whether the right button is pressed.
         */
        rbPressed: boolean;
    }
} 